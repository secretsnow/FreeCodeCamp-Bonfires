function palindrome(str){
  // Good luck!
  //Removes non-alphanumeric characters
  var tempstr = str.toLowerCase().replace(/\W|_/g, '');
  reversestring = tempstr.split("").reverse().join("");

  if (reversestring === tempstr){ 
    return true;
  }else{
    return false;
  }
}
palindrome("eye");
